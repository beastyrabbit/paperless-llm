{
  "name": "@repo/backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn main:app --reload --port 8000",
    "lint": "uv run ruff check .",
    "test": "uv run pytest",
    "format": "uv run ruff format .",
    "typecheck": "echo 'Skipping mypy - pre-existing type errors'"
  }
}
