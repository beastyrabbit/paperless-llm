{
  "name": "@paperless-llm/api",
  "private": true,
  "version": "0.0.1",
  "description": "FastAPI backend service for Paperless Local LLM",
  "scripts": {
    "dev": "cd backend && uv run uvicorn main:app --reload --port 8000",
    "build": "echo 'Python backend - no build needed'",
    "start": "cd backend && uv run uvicorn main:app --port 8000",
    "test": "cd backend && uv run pytest",
    "lint": "cd backend && uv run ruff check .",
    "typecheck": "cd backend && uv run mypy .",
    "format": "cd backend && uv run ruff format ."
  }
}
