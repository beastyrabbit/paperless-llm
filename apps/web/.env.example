# Backend API Configuration
# The frontend proxies API requests through Next.js rewrites
# This allows for relative API URLs in the client code

# Backend URL for Next.js server-side rewrites (not exposed to client)
# Python backend (port 8000) or TypeScript backend (port 8001)
BACKEND_URL=http://localhost:8001

# Legacy: Direct API URL (only needed if bypassing Next.js proxy)
# Leave empty or unset to use relative URLs with Next.js rewrites
# NEXT_PUBLIC_API_URL=
